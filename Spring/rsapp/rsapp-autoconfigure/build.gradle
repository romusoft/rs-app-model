

dependencies {

	implementation project(":rsapp-mvvm")
	//
	// process for property intellisense at compile time only
	annotationProcessor "org.springframework.boot:spring-boot-configuration-processor"
	
}

